from setuptools import setup, find_packages

setup(name='ShellCmder',
      version='0.0.1',
      description='shell cmd wrapper under linux',
      author='alvendarthy',
      author_email='alvendarthy@gmail.com',
      packages=find_packages(),
      license="GPL 2.0"
      )
